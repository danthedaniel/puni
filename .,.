import praw
import puni
import OAuth2Util
import pdb
import time

r = praw.Reddit("puni tests")
o = OAuth2Util.OAuth2Util(r)

tegc = r.get_subreddit("teaearlgraycold")

un = puni.UserNotes(r, tegc)

print(un.get_users())
